[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
markers =
    integration: tests requiring the real FAISS index or model
    slow: tests that may take significantly longer to run